



local Base = {}

function Base.new(Pos : CFrame, Stage : number) : Instance
    assert(Stage < 4, 'Internships cannot exceed 3 months.')

    local self = {}

    
    
    return self
end

return Base